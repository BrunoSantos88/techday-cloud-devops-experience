steps:

- name: 'gcr.io/cloud-builders/docker'
  entrypoint: 'sh'
  args: 
  - '-c'
  - | 
    docker build -t frontend frontend/.
    docker tag frontend:latest gcr.io/bootcamp-363315/frontend
    docker push gcr.io/bootcamp-363315/frontend
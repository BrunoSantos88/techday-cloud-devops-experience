apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: sonarqube-service
spec:
  template:
    spec:
      containers:
        - image: us-central1-docker.pkg.dev/devops-399217/sonarquebe:latest
          ports:
            - containerPort: 9000 # Porta do SonarQube
          resources:
            limits:
              memory: "4Gi" # Limite de memória
              cpu: "2" # Limite de CPU